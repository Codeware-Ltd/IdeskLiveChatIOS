import XCTest
@testable import IdeskLiveChatIOS

final class IdeskLiveChatIOSTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(IdeskLiveChatTest().text, "Hello, World!")
    }
}
